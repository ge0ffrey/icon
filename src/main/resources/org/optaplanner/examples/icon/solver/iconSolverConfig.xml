<?xml version="1.0" encoding="UTF-8"?>
<solver>
  <environmentMode>FAST_ASSERT</environmentMode>
  <solutionClass>org.optaplanner.examples.icon.domain.Schedule</solutionClass>
  <entityClass>org.optaplanner.examples.icon.domain.TaskAssignment</entityClass>

  <scoreDirectorFactory>
    <initializingScoreTrend>ONLY_DOWN</initializingScoreTrend>
    <scoreDefinitionType>HARD_SOFT_BIG_DECIMAL</scoreDefinitionType>
    <scoreDrl>org/optaplanner/examples/icon/solver/iconScoreRules.drl</scoreDrl>
  </scoreDirectorFactory>
  <termination>
    <minutesSpentLimit>5</minutesSpentLimit>
  </termination>
  
  <constructionHeuristic>
    <queuedEntityPlacer>
      <entitySelector id="placerEntitySelector">
        <cacheType>PHASE</cacheType>
        <sorterManner>DECREASING_DIFFICULTY</sorterManner>
        <selectionOrder>SORTED</selectionOrder> 
        <!-- 
            ^^^^ this is weird; don't specify any order and planner fails?
            it should be smarter than that. if it knows what it needs and I don't specify,
            it should pick the necessary setting. 
        -->
      </entitySelector>
      <cartesianProductMoveSelector>
        <changeMoveSelector>
          <entitySelector mimicSelectorRef="placerEntitySelector"/>
          <selectionOrder>RANDOM</selectionOrder>
          <valueSelector>
            <variableName>executor</variableName>
          </valueSelector>
          <selectedCountLimit>10</selectedCountLimit>
        </changeMoveSelector>
        <changeMoveSelector>
          <entitySelector mimicSelectorRef="placerEntitySelector"/>
          <selectionOrder>RANDOM</selectionOrder>
          <valueSelector>
            <variableName>startPeriod</variableName>
          </valueSelector>
          <selectedCountLimit>3</selectedCountLimit>
        </changeMoveSelector>
      </cartesianProductMoveSelector>
    </queuedEntityPlacer>
  </constructionHeuristic>
  <localSearch>
    <unionMoveSelector>
      <changeMoveSelector>
        <valueSelector>
          <variableName>startPeriod</variableName>
        </valueSelector>
      </changeMoveSelector>
      <changeMoveSelector>
        <valueSelector>
          <variableName>executor</variableName>
        </valueSelector>
      </changeMoveSelector>
      <!-- swap moves need to be filtered as:
        a/ some tasks cannot be assigned to some machines, due to more resource consumption than capacity
        b/ some periods cannot be assigned to some tasks, due to start date / deadline constraints
      -->
      <swapMoveSelector>
        <!--<filterClass>org.optaplanner.examples.icon.solver.move.ExecutorSwapMoveFilter</filterClass>-->
        <variableNameInclude>executor</variableNameInclude>
      </swapMoveSelector>
      <swapMoveSelector>
        <!--<filterClass>org.optaplanner.examples.icon.solver.move.PeriodSwapMoveFilter</filterClass>-->
        <variableNameInclude>startPeriod</variableNameInclude>
      </swapMoveSelector>
    </unionMoveSelector>
    <acceptor>
      <lateAcceptanceSize>400</lateAcceptanceSize>
      <entityTabuRatio>0.3</entityTabuRatio>
    </acceptor>
    <forager>
      <acceptedCountLimit>4</acceptedCountLimit>
    </forager>
  </localSearch>
</solver>
